apiVersion: v1
kind: Service
metadata:
  name: {{ include "administrator.fullname" . }}
  labels:
    {{- include "administrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "administrator.selectorLabels" . | nindent 4 }}
