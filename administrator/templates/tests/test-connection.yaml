apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "administrator.fullname" . }}-test-connection"
  labels:
    {{- include "administrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "administrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
