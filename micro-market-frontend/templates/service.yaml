apiVersion: v1
kind: Service
metadata:
  name: {{ include "micro-market-frontend.fullname" . }}
  labels:
    {{- include "micro-market-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micro-market-frontend.selectorLabels" . | nindent 4 }}
