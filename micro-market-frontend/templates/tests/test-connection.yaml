apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "micro-market-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "micro-market-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "micro-market-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
