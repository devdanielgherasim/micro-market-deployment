apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "micro-market.fullname" . }}-test-connection"
  labels:
    {{- include "micro-market.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "micro-market.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
