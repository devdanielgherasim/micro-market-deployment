apiVersion: v1
kind: Service
metadata:
  name: {{ include "micro-market.fullname" . }}
  labels:
    {{- include "micro-market.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micro-market.selectorLabels" . | nindent 4 }}
